// Autor SAID LAMGHARI
// Définition de la classe ClassRoom avec un export par défaut
export default class ClassRoom {
  // Constructeur de la classe, prend maxStudentsSize comme paramètre
  constructor(maxStudentsSize) {
    // Assignation de maxStudentsSize à la propriété privée _maxStudentsSize
    this._maxStudentsSize = maxStudentsSize;
  }
}
