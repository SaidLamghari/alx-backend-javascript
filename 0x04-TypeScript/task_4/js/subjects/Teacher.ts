// Définition de l'interface Teacher dans le namespace Subjects
namespace Subjects {
  export interface Teacher {
    firstName: string;
    lastName: string;
    experienceTeachingC?: number; // Expérience d'enseignement de C (optionnel)
  }
}
